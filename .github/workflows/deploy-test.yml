name: Deploy Test to test.eagleburgerband.com

on:
  push:
    branches:
      - test

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/configure-pages@v3

      - name: Install and Build
        run: |
          npm install
          npm run build
          echo "test.eagleburgerband.com" > ./build/CNAME

      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./build

      - uses: actions/deploy-pages@v4
